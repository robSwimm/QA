---
title: Basic JavaScript Variable and Function Usage Example
---
# introduction

This document explains a basic JavaScript script demonstrating variable declarations, type changes, function usage, and object manipulation.

We will cover:

1. How variables are declared and reassigned.
2. How the function is defined and used.
3. How objects are created and extended.

# variable declarations and reassignment

The script declares variables using `var` with different data types: strings, numbers, booleans, and arrays. It shows that variables can be reassigned to new values, even of different types (e.g., a string variable reassigned to a number). This illustrates JavaScript's dynamic typing.

# function definition and usage

A simple function named `add` takes two parameters and returns their sum. The function is called with arguments 3 and 4, demonstrating basic function invocation and return value usage.

# object creation and extension

An object `user` is created with properties `name` and `age`. Later, a new property `location` is added dynamically. This shows how JavaScript objects can be extended after creation.

# how to run the script

The script is a standalone JavaScript file. Run it using Node.js by executing <SwmPath>[PlayWrightAutomation/tests/demo1.js](PlayWrightAutomation/tests/demo1.js)</SwmPath> in the terminal. The console output will show the reassigned string value, the number variable, and the result of the function call if logged.

&nbsp;

*This is an auto-generated document by Swimm 🌊 and has not yet been verified by a human*

<SwmMeta version="3.0.0" repo-id="Z2l0aHViJTNBJTNBUUElM0ElM0Fyb2JTd2ltbQ==" repo-name="QA"><sup>Powered by [Swimm](https://staging.swimm.cloud/)</sup></SwmMeta>
